{
  "name": "xhr-extractor",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "npx puppeteer install --product=chrome && node server.js",
    "postinstall": "npx puppeteer install --product=chrome"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^24.0.0"
  }
}
